import { StyleSheet } from 'react-native';
import { colors } from '../../common/colors';

export const styles = StyleSheet.create({
  title: {
    color: colors.title,
    fontSize: 34,
    fontWeight: '400',
  },
  effect: {
    color: colors.title_light,
  },
});
